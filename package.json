{
  "name": "mock-rest-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "tern-on-api": "cp mockApi/db_back_up.yaml mockApi/db_stage.yaml && npx yaml-server --hotReload=off --autoStart=off --port 3000 --database ./mockApi/db_stage.yaml",
    "tests-api-run": "npx newman run petstore.collection.json -r htmlextra --reporter-htmlextra-export ./testResults/report.html",
    "cache-clean-run":"rm -rf node_modules && rm ./package-lock.json"
  },
  "keywords": ["api","mock","yaml-server","rest"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "newman-reporter-htmlextra": "^1.22.11",
    "yaml-server": "^1.10.0"
  },
  "devDependencies": {
    "newman": "^5.3.2"
  }
}
